package ExercicioLutaTurbinado;

public class TesteLuta {

	public static void main(String[] args) {
		CategoriaLuta categoriaLuta = new CategoriaLuta(1, "peso medio", 70, 80);
		CategoriaLuta categoriaLuta2 = new CategoriaLuta(2, "peso pesado", 80, 100);

		NomeLutador nomeLutador = new NomeLutador("popo", 38, 75);
		NomeLutador nomeLutador2 = new NomeLutador("caca", 25, 95);
		NomeLutador nomeLutador3 = new NomeLutador("lulu", 32, 70);

		NomeLuta luta = new NomeLuta(nomeLutador3, nomeLutador2, 10);
		
		nomeLutador.VerificaCategoria(categoriaLuta, categoriaLuta2);
		nomeLutador2.VerificaCategoria(categoriaLuta, categoriaLuta2);
		nomeLutador3.VerificaCategoria(categoriaLuta, categoriaLuta2);
		
		System.out.println(nomeLutador.getCategoriaLuta().getNomeCategoria());
	
		/*if (nomeLutador.getPeso() > categoriaLuta.getPesoMinino()
				&& nomeLutador.getPeso() > categoriaLuta.getPesoMaximo()) {
			nomeLutador.setCategoriaLuta(categoriaLuta);
		}else if (nomeLutador.getPeso() > categoriaLuta2.getPesoMinino()
				&& nomeLutador.getPeso() > categoriaLuta2.getPesoMaximo()) {
			nomeLutador.setCategoriaLuta(categoriaLuta2);
		/}else {
			
		}*/

		if (luta.confirmaLuta()) {
			System.out.println("luta confirmada");
		} else {
			System.out.println("luta invalidade, categorias diferentes");
		}
	}

}
